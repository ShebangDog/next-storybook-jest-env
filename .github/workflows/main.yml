name: run-vrt

on:
  push:
    branches:
      - "develop"
  pull_request:
    branches:
      - "develop"

  workflow_dispatch:
jobs:
  resolve-dependency:
    runs-on: ubuntu-22.04
    steps:
      - name: setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: checkout main branch
        uses: actions/checkout@v3
      - name: yarn install
        run: yarn install
      - name: ls
        run: ls
      - name: screenshot
        run: yarn screenshot
